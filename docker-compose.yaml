name: rielu-links
services:
  app:
    build: .
    ports:
      - "3000:3000"
    environment:
      DISCORD_BOT_TOKEN: "YourToken"
      DISCORD_GUILD_ID: "YourGuildID"
      DISCORD_TARGET_ROLE_ID: "YourTargetRoleID"
    networks:
      - rielu-links-network

networks:
  rielu-links-network:
    driver: bridge
