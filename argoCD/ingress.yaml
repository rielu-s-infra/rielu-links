apiVersion: traefik.io/v1alpha1
kind: IngressRoute
metadata:
  name: rielu-site
spec:
  entryPoints:
    - websecure
    - web
  routes:
    - kind: Rule
      match: Host(`rielu.uniproject.jp`) && PathPrefix(`/`)
      observability:
        accessLogs: true
        metrics: true
        tracing: true
      priority: 1
      services:
        - kind: Service
          name: rielu-site
          passHostHeader: true
          port: 3000
          responseForwarding:
            flushInterval: 1ms
          scheme: http
          sticky:
            cookie:
              httpOnly: true
              name: cookie
              secure: true
          strategy: wrr
          weight: 10
  tls:
    certResolver: cf-prod
    domains:
      - main: rielu.uniproject.jp